{
  "name": "kbo-live",
  "version": "0.1.1",
  "description": "KBO 리그 실시간 문자중계 CLI — 터미널 HUD로 투구·안타·득점을 실시간 중계 (비공식 네이버 스포츠 데이터)",
  "bin": {
    "kbo-live": "bin/kbo-live.js"
  },
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin",
    "src",
    "locales",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "start": "node bin/kbo-live.js",
    "test": "node --test test/**/*.test.js",
    "check": "node --check bin/kbo-live.js && node --check src/cli.js && node --check src/api.js && node --check src/runner.js && node --check src/broadcast.js && node --check src/render.js && node --check src/input.js && node --check src/terminal.js && node --check src/game_loader.js && node --check src/config.js && node --check src/log.js && node --check src/overwork_cli.js",
    "prepublishOnly": "npm test && npm run check"
  },
  "keywords": [
    "kbo",
    "baseball",
    "야구",
    "문자중계",
    "live",
    "cli",
    "tui",
    "scoreboard"
  ],
  "author": "lucas <lucas@withvus.ai>",
  "license": "MIT",
  "preferGlobal": true,
  "os": [
    "darwin",
    "linux",
    "win32"
  ]
}
